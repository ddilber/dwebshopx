# Copilot Instructions

## Project Guidelines
- Always use the fluent-ui-blazor MCP tools (search_components, get_component_details, get_icon_usage, etc.) when working with Fluent UI Blazor components. Never guess component APIs, parameter names, or available properties — look them up via MCP first.